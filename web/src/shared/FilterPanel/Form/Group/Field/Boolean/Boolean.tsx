import React from "react";
interface Props {
	field: {
		id: string;
		name: string;
		options: object;
		type: string;
	};
}
const Boolean = ({}: Props) => {
	return <></>;
};

export default Boolean;
