query GetCategoryFilterFields($categoryId: Int!) {
	categories(filter: { id: $categoryId }) {
		id
		filter {
			id
			groups {
				id
				name
				fields {
					id
					type
					name
					categoryField {
						id
						type
						options
					}
					options
				}
			}
		}
	}
}
