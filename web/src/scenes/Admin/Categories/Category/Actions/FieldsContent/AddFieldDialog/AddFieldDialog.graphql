mutation AddFieldToCategory(
	$categoryId: Int!
	$name: String!
	$type: String!
	$defaultValue: String!
) {
	addFieldToCategory(
		categoryId: $categoryId
		field: { name: $name, type: $type, defaultValue: $defaultValue }
	) {
		id
		fields {
			id
			name
			type
			defaultValue
		}
	}
}
