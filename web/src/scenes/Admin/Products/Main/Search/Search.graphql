query FindProductInfoByNameTemplate($template: String) {
	searchProducts(filter: { nameTemplate: $template }) {
		productsInfo {
			id
			name
			language {
				id
				code
			}
			product {
				id
			}
		}
	}
}
